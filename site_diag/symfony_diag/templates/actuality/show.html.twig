{% extends 'base.html.twig' %}

{% block title %}Actuality{% endblock %}

{% block body %}
    <h1>Actuality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actuality.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ actuality.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ actuality.Description }}</td>
            </tr>
            <tr>
                <th>Importance</th>
                <td>{{ actuality.Importance }}</td>
            </tr>
            <tr>
                <th>DateAj</th>
                <td>{{ actuality.DateAj ? actuality.DateAj|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_actuality_index') }}">back to list</a>

    <a href="{{ path('app_actuality_edit', {'id': actuality.id}) }}">edit</a>

    {{ include('actuality/_delete_form.html.twig') }}
{% endblock %}
